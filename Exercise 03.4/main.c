#include <stdio.h>

int main(){
    float result;
    result=7.0/22.0;
    printf("The result is %d", result); // Because %d is looking to print out an integer, and 'result' is declared a float, this line prints a random integer value
    return 0;
}
